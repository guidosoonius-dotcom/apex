[project]
name = "apex"
version = "0.1.0"
requires-python = ">=3.9"
dependencies = [
    "flask==3.0.3",
    "python-docx==1.1.2",
    "python-pptx==0.6.23",
]

[tool.vercel]
entrypoint = "api.index:app"
